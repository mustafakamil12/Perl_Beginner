use strict;
use warnings;
use Getopt::long;

$|=1;

sub main{
    my %firstHash = (userid => undef,
                    serverid => undef,
                    env => undef);

print "\%firstHash = %firstHash\n";

# GetOptions = (\%firstHash,
#     "csi=s",
#     "server=s"
# );        


# print GetOptions . "\n";
}

main();